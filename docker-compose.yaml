services:
  app-dev:
    build:
      context: .
      target: dev
    init: true
    ports:
      - 8000:8000
    volumes:
      - ./app:/app:ro
  app-linting_testing:
    build:
      context: .
      target: linting_testing
    init: true
    ports:
      - 8000:8000
